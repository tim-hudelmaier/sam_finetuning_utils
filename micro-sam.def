Bootstrap: docker
From: condaforge/miniforge3:latest

%post -c /bin/bash
  git clone https://github.com/tim-hudelmaier/micro-sam.git
  cd micro-sam
  mamba env create -f environment_gpu.yaml
  mamba run -n sam pip install -e .
  mamba run -n sam pip install bioio bioio_ome_tiff einops loguru fire
  apt-get -y update && apt-get install -y li
